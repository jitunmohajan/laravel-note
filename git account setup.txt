first account>>
git config --global user.email "myemail which uses on my account"
git config --global user.name "username"

* configure another git account>>first add ssh key

git config --global user.email "myemail which uses on my account"
git config --global user.name "username"
git remote set-url origin git@github.com:jitundark/test.git
